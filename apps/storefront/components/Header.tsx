'use client';
import Link from 'next/link';
import {useEffect,useMemo,useState} from 'react';
import {useRouter} from 'next/navigation';
import {api,getCartToken,getCurrency,setCurrency,getLocale,setLocale,STORE,mediaUrl,money} from '../lib/api';

type MenuNode={id?:string;label:string;href:string;children:MenuNode[]};
function Icon({name}:{name:'search'|'user'|'heart'|'compare'|'cart'|'menu'|'close'|'chevron'|'trash'}){const paths:any={search:<><circle cx="11" cy="11" r="6.5"/><path d="m16 16 4 4"/></>,user:<><circle cx="12" cy="8" r="3.5"/><path d="M4.5 20c.7-4.1 3.3-6.2 7.5-6.2s6.8 2.1 7.5 6.2"/></>,heart:<path d="M12 20.5 4.8 13.7C.7 9.8 2.8 4 7.5 4c2 0 3.4 1 4.5 2.4C13.1 5 14.5 4 16.5 4c4.7 0 6.8 5.8 2.7 9.7L12 20.5Z"/>,compare:<><path d="M7 4v16M17 4v16"/><path d="m3.5 7 3.5-3 3.5 3M13.5 17l3.5 3 3.5-3"/></>,cart:<><path d="M3 4h2l2.2 10.2h9.7L20 7H6.2"/><circle cx="9" cy="19" r="1.2"/><circle cx="17" cy="19" r="1.2"/></>,menu:<><path d="M4 7h16M4 12h16M4 17h16"/></>,close:<><path d="m6 6 12 12M18 6 6 18"/></>,chevron:<path d="m9 6 6 6-6 6"/>,trash:<><path d="M5 7h14M9 7V4h6v3M7 7l1 13h8l1-13"/></>};return <svg aria-hidden="true" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.7" strokeLinecap="round" strokeLinejoin="round">{paths[name]}</svg>}
function menuTree(raw:any[],categories:any[]):MenuNode[]{if(!raw.length)return [{label:'Tüm Ürünler',href:'/products',children:categories.slice(0,8).map((x:any)=>({label:x.name,href:'/products?category='+encodeURIComponent(x.slug),children:[]}))},{label:'Yeni Gelenler',href:'/products?sort=newest',children:[]},{label:'Blog',href:'/blog',children:[]}];const normalize=(x:any):MenuNode=>({id:String(x.id||''),label:x.label||x.title||x.name||'Menü',href:x.href||x.url||x.path||'/',children:Array.isArray(x.children)?x.children.map(normalize):[]});const rows=raw.map(normalize);if(rows.some((x:any)=>x.children.length))return rows;const byId=new Map(rows.map(x=>[x.id,x]));const roots:MenuNode[]=[];raw.forEach((source:any,i:number)=>{const item=rows[i];const parent=byId.get(String(source.parentId||source.parentItemId||''));if(parent)parent.children.push(item);else roots.push(item)});return roots}

export default function Header({boot}:{boot:any}){
  const router=useRouter();const[count,setCount]=useState(0);const[currency,setCur]=useState(getCurrency());const[locale,setLoc]=useState(getLocale());const[searchOpen,setSearchOpen]=useState(false);const[mobileOpen,setMobileOpen]=useState(false);const[cartOpen,setCartOpen]=useState(false);const[q,setQ]=useState('');const[results,setResults]=useState<any[]>([]);const[searchBusy,setSearchBusy]=useState(false);const[cart,setCart]=useState<any>(null);const[cartBusy,setCartBusy]=useState(false);
  const store=boot?.store||{};const currencies=store.currencies||[];const locales=store.locales||[];const header=boot?.design?.header||{};const categories=Array.isArray(boot?.categories)?boot.categories:[];
  const menuItems=useMemo(()=>{const menus=Array.isArray(boot?.menus)?boot.menus:[];const menu=menus.find((m:any)=>['header','main','primary'].includes(String(m.handle||'').toLowerCase()))||menus[0];return menuTree(Array.isArray(menu?.items)?menu.items:[],categories)},[boot?.menus,categories]);
  async function sync(){const t=getCartToken();if(!t){setCount(0);setCart(null);return}try{const c=await api('/storefront/carts/'+t);setCart(c);setCount(c.items?.reduce((a:number,x:any)=>a+x.quantity,0)||0);if(c.currency){setCur(c.currency);if(c.currency!==getCurrency())setCurrency(c.currency)}}catch{setCount(0);setCart(null)}}
  useEffect(()=>{setCur(getCurrency());setLoc(getLocale());void sync();const onCart=()=>void sync();window.addEventListener('cart-change',onCart);return()=>window.removeEventListener('cart-change',onCart)},[]);
  useEffect(()=>{if(!searchOpen||q.trim().length<2){setResults([]);setSearchBusy(false);return}setSearchBusy(true);const timer=window.setTimeout(()=>{api(`/storefront/${STORE}/products?q=${encodeURIComponent(q.trim())}&limit=6&currency=${encodeURIComponent(currency)}&locale=${encodeURIComponent(locale)}`).then(x=>setResults(x.items||[])).catch(()=>setResults([])).finally(()=>setSearchBusy(false))},220);return()=>window.clearTimeout(timer)},[q,searchOpen,currency,locale]);
  function changeLocale(code:string){setLoc(code);setLocale(code)}async function changeCurrency(code:string){setCur(code);setCurrency(code);const t=getCartToken();if(t){try{await api('/storefront/carts/'+t+'/currency',{method:'PATCH',body:JSON.stringify({currency:code})});window.dispatchEvent(new Event('cart-change'))}catch{}}}
  function submitSearch(e:React.FormEvent){e.preventDefault();const term=q.trim();setSearchOpen(false);router.push(term?'/products?q='+encodeURIComponent(term):'/products')}
  async function updateCart(itemId:string,quantity:number){const token=getCartToken();if(!token||cartBusy)return;setCartBusy(true);try{const next=await api(`/storefront/carts/${token}/items/${itemId}`,{method:'PATCH',body:JSON.stringify({quantity})});setCart(next);setCount(next.items?.reduce((a:number,x:any)=>a+x.quantity,0)||0);window.dispatchEvent(new Event('cart-change'))}finally{setCartBusy(false)}}
  async function openCart(){setCartOpen(true);await sync()}
  const logo=<Link className="header__heading-link header__logo-link clear-underline" href="/" aria-label={store.name||'Anasayfa'}>{store.logoUrl?<img className="header__heading-logo" src={store.logoUrl} alt={store.name||'Logo'} decoding="async" fetchPriority="high"/>:<span className="h4">{store.name||'Mağaza'}</span>}</Link>;
  const menu=<nav className="desktop-main-menu list-menu--classic" aria-label="Ana menü"><ul className="list-menu list-menu--inline unstyle-ul">{menuItems.map((x,i)=><li className="list-menu__item" key={x.id||i}><Link className="header__menu-item link animation-underline" href={x.href}>{x.label}{x.children.length>0&&<span aria-hidden="true">⌄</span>}</Link>{x.children.length>0&&<div className="header__submenu list-menu__submenu"><div className="page-width"><div className="page-grid-4">{x.children.map((child,j)=><div key={child.id||j}><Link className="h5 clear-underline" href={child.href}>{child.label}</Link>{child.children.length>0&&<ul className="unstyle-ul mt10">{child.children.map((leaf,k)=><li key={leaf.id||k}><Link className="link link__base-to_accent-color" href={leaf.href}>{leaf.label}</Link></li>)}</ul>}</div>)}</div></div></div>}</li>)}</ul></nav>;
  const tools=<div className="header__icons header__user-icons display-flex align-items-center"><button className="header__icon header__icon--search" type="button" aria-label="Ara" onClick={()=>setSearchOpen(true)}><Icon name="search"/></button><Link className="header__icon header__icon--account" href="/account" aria-label="Hesabım"><Icon name="user"/></Link><Link className="header__icon header__icon--wishlist" href="/account?tab=wishlist" aria-label="Favoriler"><Icon name="heart"/></Link><button className="header__icon header__icon--cart" type="button" onClick={()=>void openCart()} aria-label={`Sepet, ${count} ürün`}><Icon name="cart"/>{count>0&&<span className="cart-count-bubble">{count}</span>}</button><button className="header__icon header__icon--menu hide-desktop" type="button" onClick={()=>setMobileOpen(true)} aria-label="Menü"><Icon name="menu"/></button></div>;
  return <>
    <header id="theme-section-ticarti-header" data-section-name="header-layout-classic" className="section-header">
      <div className="header__mobile section-background"><div className="page-width header-mobile__container display-flex align-items-center"><button className="header__icon" type="button" onClick={()=>setMobileOpen(true)} aria-label="Menü"><Icon name="menu"/></button>{logo}<div className="header-mobile__icons">{tools}</div></div></div>
      <div className={`header__desktop header__desktop__shadow ${header.sticky===false?'':'sticky-header'}`}>
        {header.topbarEnabled&&<div className="header__topbar section-background"><div className="page-width text-center body3">{header.topbarLink?<Link href={header.topbarLink}>{header.topbarText||'Duyuru'}</Link>:<span>{header.topbarText||'Duyuru'}</span>}</div></div>}
        <div className="header__main header-layout-classic section-background"><div className="page-width"><div className="header logo-to-left top-menu-to-center">{logo}{menu}{tools}</div></div></div>
      </div>
    </header>
    {searchOpen&&<div className="popup-modal predictive-search-modal" role="dialog" aria-modal="true" aria-label="Arama"><div className="popup-modal__overlay" onClick={()=>setSearchOpen(false)}/><div className="popup-modal__content"><button className="popup-modal__close" type="button" aria-label="Kapat" onClick={()=>setSearchOpen(false)}><Icon name="close"/></button><form className="predictive-search__form" onSubmit={submitSearch}><div className="field field-with-icon"><input className="field__input" autoFocus value={q} onChange={e=>setQ(e.target.value)} placeholder="Ne aramıştınız?"/><button className="field__icon" aria-label="Ara"><Icon name="search"/></button></div></form><div className="predictive-search__results">{searchBusy&&<p>Aranıyor…</p>}{results.map((p:any)=><Link className="predictive-search__item" onClick={()=>setSearchOpen(false)} href={'/products/'+p.slug} key={p.id}>{p.images?.[0]?.url&&<img src={mediaUrl(p.images[0].url,'thumb','webp')} alt=""/>}<span><b>{p.title}</b><small>{money(p.variants?.[0]?.price,p.displayCurrency||currency)}</small></span></Link>)}</div></div></div>}
    {mobileOpen&&<div className="popup-modal popup-modal__left mobile-menu-modal" role="dialog" aria-modal="true"><div className="popup-modal__overlay" onClick={()=>setMobileOpen(false)}/><div className="popup-modal__content popup-modal__content__f-height"><div className="popup-modal__content__header"><b>Menü</b><button onClick={()=>setMobileOpen(false)} aria-label="Kapat"><Icon name="close"/></button></div><div className="popup-modal__content__data custom__scrollbar"><ul className="unstyle-ul mobile-menu__list">{menuItems.map((x,i)=><li key={x.id||i}><Link onClick={()=>setMobileOpen(false)} href={x.href}>{x.label}</Link>{x.children.length>0&&<ul className="unstyle-ul">{x.children.map((c,j)=><li key={c.id||j}><Link onClick={()=>setMobileOpen(false)} href={c.href}>{c.label}</Link></li>)}</ul>}</li>)}</ul><div className="mobile-menu__selectors">{locales.length>1&&<select aria-label="Dil" value={locale} onChange={e=>changeLocale(e.target.value)}>{locales.map((l:any)=><option key={l.locale} value={l.locale}>{l.label}</option>)}</select>}{currencies.length>1&&<select aria-label="Para birimi" value={currency} onChange={e=>void changeCurrency(e.target.value)}>{currencies.map((c:any)=><option key={c.code} value={c.code}>{c.code}</option>)}</select>}</div></div></div></div>}
    {cartOpen&&<div className="popup-modal popup-modal__right cart-drawer-modal" role="dialog" aria-modal="true"><div className="popup-modal__overlay" onClick={()=>setCartOpen(false)}/><div className="popup-modal__content popup-modal__content__f-height"><div className="popup-modal__content__header"><h2 className="h4">Sepet</h2><button onClick={()=>setCartOpen(false)} aria-label="Kapat"><Icon name="close"/></button></div><div className="popup-modal__content__data custom__scrollbar">{cart?.items?.length?cart.items.map((i:any)=><div className="cart-item" key={i.id}><div className="cart-item__media">{i.variant?.product?.images?.[0]?.url&&<img src={i.variant.product.images[0].url} alt=""/>}</div><div className="cart-item__details"><Link href={`/products/${i.variant.product.slug}`} onClick={()=>setCartOpen(false)}>{i.variant.product.title}</Link><small>{i.variant.title}</small><div className="quantity field"><button className="quantity__button" disabled={cartBusy||i.quantity<=1} onClick={()=>void updateCart(i.id,i.quantity-1)}>−</button><span className="quantity__input">{i.quantity}</span><button className="quantity__button" disabled={cartBusy} onClick={()=>void updateCart(i.id,i.quantity+1)}>+</button></div></div><div className="cart-item__totals">{money(Number(i.variant.price)*i.quantity,cart.currency||currency)}<button aria-label="Kaldır" disabled={cartBusy} onClick={()=>void updateCart(i.id,0)}><Icon name="trash"/></button></div></div>):<div className="cart__warnings text-center"><p className="h4">Sepetiniz boş</p></div>}</div><div className="cart-drawer__footer"><Link className="btn btn-card-width" href="/cart" onClick={()=>setCartOpen(false)}>Sepete git</Link></div></div></div>}
  </>;
}
