'use client';import {FormEvent,useState} from 'react';import Link from 'next/link';
const API=process.env.NEXT_PUBLIC_API_URL||'/api/v1';
export default function Forgot(){const[busy,setBusy]=useState(false);const[msg,setMsg]=useState('');const[err,setErr]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setErr('');try{const f=new FormData(e.currentTarget);const r=await fetch(API+'/merchant-auth/password/forgot',{method:'POST',credentials:'include',headers:{'content-type':'application/json','x-auth-client':'merchant-landing'},body:JSON.stringify({email:String(f.get('email')||'').trim()})});const j=await r.json().catch(()=>({}));if(!r.ok)throw new Error(j?.message||'İşlem başarısız');setMsg('Eğer bu e-posta ile kayıtlı bir hesap varsa sıfırlama bağlantısı gönderildi.')}catch(e:any){setErr(e.message)}finally{setBusy(false)}}return <main className="ticarti-auth-simple"><div className="ticarti-auth-box"><a className="ticarti-logo" href="https://ticarti.com">ticarti<span>.</span></a><h1>Şifrenizi sıfırlayın</h1><p>Hesabınızdaki e-posta adresini yazın.</p>{msg&&<div className="ticarti-auth-success">{msg}</div>}{err&&<div className="ticarti-auth-error">{err}</div>}<form onSubmit={submit}><label>E-posta<input name="email" type="email" required/></label><button className="ticarti-btn primary full" disabled={busy}>{busy?'Gönderiliyor…':'Sıfırlama bağlantısı gönder'}</button></form><Link href="/">← Girişe dön</Link></div></main>}
