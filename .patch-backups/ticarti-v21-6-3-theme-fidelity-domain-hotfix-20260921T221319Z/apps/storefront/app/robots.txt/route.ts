import {NextRequest} from 'next/server';
const API=process.env.NEXT_PUBLIC_API_URL||process.env.API_URL||'https://dev.ticarti.com/v1';
function host(req:NextRequest){return String(req.headers.get('x-forwarded-host')||req.headers.get('host')||'').split(',')[0].trim().toLowerCase().replace(/:\d+$/,'')}
function key(h:string){if(h.endsWith('.ticarti.com'))return h.slice(0,-'.ticarti.com'.length).split('.')[0];return h}
export async function GET(req:NextRequest){const h=host(req);if(!h||['ticarti.com','www.ticarti.com','login.ticarti.com','app.ticarti.com','academy.ticarti.com','superadmin.ticarti.com','dev.ticarti.com'].includes(h))return new Response('User-agent: *\nAllow: /\n',{headers:{'content-type':'text/plain; charset=utf-8'}});let cfg:any={};try{const r=await fetch(`${API}/storefront/${encodeURIComponent(key(h))}`,{cache:'no-store'});if(r.ok)cfg=await r.json()}catch{}const seo=cfg?.store?.settings?.seoAdvanced||{};const robots=String(seo?.technical?.robots||'index,follow').toLowerCase();const noindex=robots.includes('noindex');const body=`User-agent: *\n${noindex?'Disallow: /':'Allow: /'}\nSitemap: https://${h}/sitemap.xml\n`;return new Response(body,{headers:{'content-type':'text/plain; charset=utf-8','cache-control':'public,max-age=300'}})}
