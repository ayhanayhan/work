import preset from './home-preset.json';

function blockToItem(id:string,b:any){const s=b?.settings||{};return {id,type:b?.type,title:s.heading_text||s.title||'',subtitle:s.subheading_text||s.subtitle||'',content:s.desc||s.description||'',buttonText:s.button_name||s.link_name||'',linkUrl:s.url||s.button_url||'',imageUrl:s.image||s.image_url||'',videoUrl:s.video_url||'',...s}}
export function sourceHomePreset(){const p:any=preset;return (p.order||[]).map((id:string)=>{const section=p.sections?.[id]||{};const bs=section.blocks||{};const items=(section.block_order||Object.keys(bs)).map((bid:string)=>blockToItem(bid,bs[bid]));const s=section.settings||{};return {id,sectionType:section.type,enabled:section.disabled!==true,settings:{...s,title:s.main_heading||s.heading_text||s.title||'',subtitle:s.main_subheading||s.subheading_text||s.subtitle||'',content:s.main_desc||s.desc||s.description||'',buttonText:s.link_name||s.button_name||'',linkUrl:s.url||'',heightDesktop:s.desktop_image_height||s.image_height||620,count:s.product_limit||s.post_limit||s.grid||8,items},blocks:bs}})}
